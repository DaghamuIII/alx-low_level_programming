#include "main.h"

/**
* _strlen - calculate the length of a string s
* @s: a pointer to string whose length is calculates
* return: (0) success
*/
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
		;
	return (c);
}
